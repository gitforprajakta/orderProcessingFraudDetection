{
  "name": "order-processing-fraud-detection",
  "version": "0.1.0",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "build": "tsc -p infra/tsconfig.json",
    "synth": "cdk synth",
    "deploy": "cdk deploy && aws cloudformation describe-stacks --stack-name OrderProcessingFraudDetectionStack --query \"Stacks[0].Outputs\" --output json > stack-outputs.json && STACK_OUTPUTS_FILE=stack-outputs.json node scripts/write-frontend-config.js",
    "frontend:config": "node scripts/write-frontend-config.js"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "aws-cdk": "^2.200.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.200.0",
    "constructs": "^10.4.2"
  }
}

