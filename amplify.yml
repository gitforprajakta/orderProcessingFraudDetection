version: 1
applications:
  - appRoot: .
    frontend:
      phases:
        build:
          commands: []
      artifacts:
        baseDirectory: frontend
        files:
          - "**/*"
      cache:
        paths: []
